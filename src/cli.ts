#!/usr/bin/env node

// Legacy stub — HIRAM is controlled via Telegram (Secretary) and JIRA board.
// This CLI client is unused.
console.log('HIRAM is controlled via Telegram and the JIRA board. This CLI is not implemented.');
process.exit(0);
